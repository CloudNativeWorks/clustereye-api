# ClusterEye API Server Environment Variables

# JWT Secret Key - ÜRETİMDE MUTLAKA DEĞİŞTİRİN!
# En az 32 karakter uzunluğunda güçlü bir anahtar kullanın
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-32chars-min

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=clustereye
DB_USER=clustereye_user
DB_PASSWORD=your_db_password

# InfluxDB Configuration
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=your_influxdb_token
INFLUXDB_ORG=clustereye
INFLUXDB_BUCKET=clustereye

# Server Configuration
HTTP_PORT=8080
GRPC_PORT=50051

# Log Level (debug, info, warn, error)
LOG_LEVEL=info

# Rate Limiting (requests per minute)
RATE_LIMIT=120

# Security Settings
ENABLE_HTTPS=false
SSL_CERT_PATH=/path/to/cert.pem
SSL_KEY_PATH=/path/to/key.pem

# Encryption Settings
# CRITICAL: Change this in production! Must be exactly 32 bytes
ENCRYPTION_KEY=clustereye-default-32byte-key!!! 